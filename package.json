{
  "name": "cat-maps",
  "private": true,
  "packageManager": "npm@10.2.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.3"
  },
  "workspaces": [
    "apps/*"
  ]
}
